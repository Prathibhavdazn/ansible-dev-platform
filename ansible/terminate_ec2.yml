- name: Terminate EC2 instance
  hosts: localhost
  connection: local
  gather_facts: false

  tasks:
    - name: Terminate EC2
      amazon.aws.ec2_instance:
        instance_ids: i-06548b7dd39eb7987 # ← Replace with your EC2 instance ID
        state: absent
        region: us-east-1                  # ← Replace with your region
